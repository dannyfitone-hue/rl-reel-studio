import Link from 'next/link'
import Header from './components/Header'
import {industries,styles,outputs} from '@/lib/data'
export default function Home(){return <div className="shell"><Header/><section className="hero"><div><div className="eyebrow">AI editing platform powered by RL Footage style</div><h1>Upload footage. Get premium reels ready to post.</h1><p>RL Reel Studio helps restaurants, realtors, mortgage companies, med spas, dentists, auto dealers, event planners, gyms and small businesses turn raw footage into professional social media content automatically.</p><div className="heroActions"><Link className="btn btnGold" href="/projects/new">Start New Project</Link><Link className="btn btnDark" href="/dashboard">View Dashboard</Link></div><div className="chiprow" style={{marginTop:30}}>{outputs.slice(0,5).map(o=><span className="chip" key={o}>{o}</span>)}</div></div><div className="studioCard glass"><div className="studioScreen"><div className="eyebrow">Project Processing</div><h2 style={{fontSize:32,margin:'12px 0'}}>Luxury Restaurant Reel</h2><p className="muted">AI is analyzing footage, finding the best clips, applying RL Luxury pacing, captions, music direction, and commercial structure.</p><div className="timeline"><div><span className="muted">Analyzing footage</span><div className="bar"><div className="fill" style={{width:'92%'}}/></div></div><div><span className="muted">Selecting strongest clips</span><div className="bar"><div className="fill" style={{width:'78%'}}/></div></div><div><span className="muted">Building reel sequence</span><div className="bar"><div className="fill" style={{width:'64%'}}/></div></div><div><span className="muted">Applying RL Style</span><div className="bar"><div className="fill" style={{width:'48%'}}/></div></div></div><div className="chiprow" style={{marginTop:26}}><span className="chip">Version A</span><span className="chip">Version B</span><span className="chip">Version C</span></div></div></div></section><section id="how" className="section"><h2>How it works</h2><div className="grid">{['Upload phone or professional footage','Select your business type','Choose the RL editing style','Pick output format','AI builds preview versions','Export final video'].map((x,i)=><div className="card" key={x}><div className="eyebrow">Step {i+1}</div><h3>{x}</h3><p>Built for fast, premium social content without waiting days for manual editing.</p></div>)}</div></section><section id="styles" className="section"><h2>RL editing styles</h2><div className="grid">{styles.map(s=><div className="card" key={s}><h3>{s}</h3><p>{s==='RL Luxury'?'Premium cinematic pacing for luxury brands.':s==='RL Commercial'?'Sales-focused structure with strong hooks and CTA.':s==='RL High Energy'?'Fast cuts, movement, and social-first attention.':s==='RL Product Showcase'?'Perfect for food, jewelry, caviar, beauty, and retail products.':s==='RL Storytelling'?'Emotional story flow that makes people watch longer.':'Movie-style editing for high-end visuals.'}</p></div>)}</div></section><section className="section"><h2>Industries ready for launch</h2><div className="chiprow">{industries.map(i=><span className="chip" key={i}>{i}</span>)}</div></section></div>}
