'use client'
import {useState} from 'react'
import Sidebar from '../../components/Sidebar'
import {industries,styles,outputs} from '@/lib/data'
export default function NewProject(){const[step,setStep]=useState(1);return <div className="dash"><Sidebar/><main className="main"><div className="toprow"><div><div className="eyebrow">Create Project</div><h1 style={{fontSize:44,margin:'8px 0'}}>Build a new RL Reel</h1><p className="muted">Upload footage, select industry, choose RL style, and pick final output formats.</p></div><button className="btn btnGold" onClick={()=>setStep(Math.min(4,step+1))}>{step<4?'Continue':'Generate Preview'}</button></div><div className="panel"><div className="chiprow"><span className="chip">1 Project Info</span><span className="chip">2 Upload</span><span className="chip">3 Style</span><span className="chip">4 Output</span></div>{step===1&&<div style={{marginTop:24}}><h2>Project details</h2><div className="formGrid"><input placeholder="Project name, example: Summer Restaurant Promo"/><select><option>Select business type</option>{industries.map(i=><option key={i}>{i}</option>)}</select><textarea placeholder="Notes, goals, offer, hook, CTA, brand direction" style={{gridColumn:'1/-1',minHeight:120}}/></div></div>}{step===2&&<div style={{marginTop:24}}><h2>Upload footage</h2><div className="uploadBox"><h3>Drag your footage here</h3><p className="muted">Phone videos, professional footage, drone shots, b-roll, logo files, product shots, and music references.</p><button className="btn btnGold">Browse Files</button></div></div>}{step===3&&<div style={{marginTop:24}}><h2>Select RL style</h2><div className="selectGrid">{styles.map(s=><div className="selectCard" key={s}><h3>{s}</h3><p className="muted">Apply {s} pacing, structure, text placement, transitions, and content flow.</p></div>)}</div></div>}{step===4&&<div style={{marginTop:24}}><h2>Select output types</h2><div className="selectGrid">{outputs.map(o=><div className="selectCard" key={o}><h3>{o}</h3><p className="muted">Optimized format, duration, framing, captions, and export settings.</p></div>)}</div><div className="panel"><h2>Processing preview</h2><p className="muted">Once connected to backend AI/FFmpeg, this will generate Version A, Version B, and Version C for preview.</p><div className="progressLine"><span/></div></div></div>}</div></main></div>}
